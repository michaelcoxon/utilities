import { IType } from "./_types";

let __nullType;
const NullType: IType = __nullType = Object.seal({
    fullName: 'Array',
    name: 'Array',
    namespace: '',
    baseType: __nullType,
    isArray: false,
    factory: () => null,
});

export default NullType;